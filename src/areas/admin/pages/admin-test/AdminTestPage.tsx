import { Box, Typography } from '@mui/material';

export const AdminTestPage = () => {
  return (
    <Box sx={{ height: '100%', display: 'flex', alignItems: 'center', justifyContent: 'center' }}>
      <Box>
        <Typography variant="h2">Admin Test Page</Typography>
      </Box>
    </Box>
  );
};
